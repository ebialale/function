const numbers = [3, 5, 6, 7, 3, 4, 7];


var max = numbers.length;
//var sum = numbers[i];

var ans = numbers[0] + numbers[numbers.length - 1]

for (var i = 0; i < max; i++) {
    
   console.log(numbers[i]);
}





numbers.forEach(function(num) {
    console.log(num)
})
